head
  def PLATFORMS
    tablet = when screenWidth less then 900
    mobile = when screenWidth less then 450
  end
  def GLOBAL_STYLES
    forAll = new style {
      margin: 0,
      padding: 0,
    }
    for all elements apply forAll
    for all p elements apply new style fontFamily: 'verdana'
  end
end

body = new div in window
body apply new style {
  color: white,
  bg: black,
  width: 100% of parentValue max 450px,
  in mobile overwrite {
    width: 90% of parentValue
  }
}